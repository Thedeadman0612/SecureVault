# Phase 1 dependencies for SecureVault

# Web framework and ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Templating
jinja2>=3.1.4

# Form data parsing (required by FastAPI for HTML form submissions)
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Validation and settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Environment variable loading
python-dotenv>=1.0.0

# Encryption (Fernet + PBKDF2HMAC)
cryptography>=42.0.0

# Password hashing (Argon2)
argon2-cffi>=23.1.0

# Session signing (used by Starlette SessionMiddleware)
itsdangerous>=2.2.0

# TOTP two-factor authentication (Phase 3)
pyotp>=2.9
qrcode[pil]>=7.4

# Testing
pytest>=8.2.0
httpx>=0.27.0
